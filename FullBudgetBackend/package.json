{
  "name": "fullbudget-backend",
  "version": "1.1.0",
  "description": "FullBudget AI Coach backend - serves the frontend and proxies AI Coach questions to OpenAI without ever exposing the API key to the client.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "test:express": "node --test test/express.integration.test.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
